spring:
  liquibase:
    change-log: classpath:db/changelog/table.xml
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://db:5432/mini-mail
  thymeleaf:
    prefix: classpath:/client-templates/

